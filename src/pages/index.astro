---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import NavBar from '../components/NavBar.astro';
import 'aos/dist/aos.css';
---

<Layout title="Kailand">
	<NavBar />
	<main>
		<div class="h-20"></div>
		<h1 data-aos="fade-zoom-in" data-aos-delay="200">Bienvenido a <span class="text-gradient animation-parpadeo">Kailand</span></h1>
		<p class="instructions" data-aos="fade-right">
			¡Bienvenido a <code>Kailand!</code>, un equipo profesional y dedicado a la creación de contenido. Aunque ya no somos un servidor público, seguimos comprometidos en ofrecerte el máximo entretenimiento a través de contenido de alta calidad. ¡Síguenos en nuestras redes para estar al tanto de todas las novedades!
		</p>
		<ul role="list" class="link-card-grid h-40" style="margin-bottom: 2rem;">
			<Card
				href="https://discord.com/invite/chwAE86T6W"
				title="Discord"
				body="Servidor oficial de Kailand, únete y vive una experiencia inolvidable."
				animation='fade-right'
				typetarget='_blank'
			/>
		</ul>
		<ul role="list" class="link-card-grid">			
			<Card
				href='https://linktr.ee/gatoartstudio'
				title='Developer'
				body='Aca encontraras todas las redes sociales y contactos del desarrollador del proyecto.'
				animation='fade-right'
				typetarget='_blank'
			/>
			<Card
				href='/redes'
				title='Redes'
				body='Redes sociales y contactos de Kailand.'
				animation='fade-left'
				typetarget='_self'
			/>
		</ul>
		<script is:inline src="https://unpkg.com/aos@next/dist/aos.js"></script>
		<script src="../js/main.js"> </script>
	</main>
</Layout>

<style>
	main {
		margin: auto;
		padding: 1rem;
		width: 800px;
		max-width: calc(100% - 2rem);
		color: white;
		font-size: 20px;
		line-height: 1.6;
	}
	@keyframes parpadeo {
		0%, 50%, 100% {
			opacity: 1;
		}
		25%, 75% {
			opacity: 0.5;
		}
	}
	.animation-parpadeo {
		animation: parpadeo 3s infinite;
	}
	.astro-a {
		position: absolute;
		top: -32px;
		left: 50%;
		transform: translatex(-50%);
		width: 220px;
		height: auto;
		z-index: -1;
	}
	h1 {
		font-size: 4rem;
		font-weight: 700;
		line-height: 1;
		text-align: center;
		margin-bottom: 1em;
		text-shadow: 0 0 50px rgba(0, 255, 0, 0.5);
	}
	.text-gradient {
		background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}
	.instructions {
		margin-bottom: 2rem;
		border: 1px solid rgba(var(--accent-light), 25%);
		background: linear-gradient(
			217deg,
			rgb(9, 212, 9),
			rgba(255, 0, 0, 0) 70.71%),
			linear-gradient(127deg, rgb(49, 172, 33), rgba(0, 255, 0, 0) 70.71%),
			linear-gradient(336deg, rgb(16, 49, 14), rgba(0, 0, 255, 0) 70.71%);
		padding: 1.5rem;
		border-radius: 8px;
		box-shadow: 0 3px 20px rgba(31, 255, 1, 0.226);
	}
	.instructions code {
		font-size: 0.8em;
		font-weight: bold;
		background: rgba(var(--accent-light), 12%);
		color: rgb(var(--accent-light));
		border-radius: 4px;
		padding: 0.3em 0.4em;
	}
	.instructions strong {
		color: rgb(var(--accent-light));
	}
	.descripcion {
		margin-bottom: 2rem;
		border: 1px solid rgba(var(--accent-light), 25%);
		background: linear-gradient(rgba(var(--accent-dark), 66%), rgba(var(--accent-dark), 33%));
		padding: 1.5rem;
		border-radius: 8px;
	}
	.descripcion code {
		font-size: 0.8em;
		font-weight: bold;
		background: rgba(var(--accent-light), 12%);
		color: rgb(var(--accent-light));
		border-radius: 4px;
		padding: 0.3em 0.4em;
	}
	.descripcion strong {
		color: rgb(var(--accent-light));
	}
	.link-card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
		gap: 2rem;
		padding: 0;
	}
</style>
